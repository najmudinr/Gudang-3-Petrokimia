import 'package:flutter/material.dart';

class DetailReportShiftPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Report Shift'),
      ),
      body: Center(
        child: Text('Ini adalah halaman detail report shift'),
      ),
    );
  }
}